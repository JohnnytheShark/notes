name: Test Release Version 

on:
    workflow_dispatch

jobs: 
    Test-Release: 
        runs-on: ubuntu-latest
        steps: 
            - name: Echo_Reference
              run: echo ${{github.ref_name}}
